

-> This level of folder(OnBoard) works on client machines like (low power constraint devices)
-> Before running the below said python script, please make sure python-charm(crypo library) is installed.
-> If py-charm not installed, do follow instruction from charm-dev folder ( ../../char-dev) that builds from source.
-> Now at this level we running client application code.

#1. Running onboard_vocal_cpabe.py script will receive commands from master PC or laptops that simulates vATC virtual a    ir traffic controller. This in-turn is mapped UAS flight controller commands.

